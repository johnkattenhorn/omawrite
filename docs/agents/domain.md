# Domain Docs

Before exploring the codebase, read `CONTEXT.md` and relevant ADRs in
`docs/adr/` when they exist. If they do not exist, proceed normally.

This is a single-context repository. Use terminology defined in `CONTEXT.md`.
Surface conflicts with an existing ADR instead of silently overriding it.
